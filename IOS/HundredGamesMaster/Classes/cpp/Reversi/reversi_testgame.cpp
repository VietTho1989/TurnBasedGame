#include <string>
#include <cstdlib>
using namespace std;

namespace Reversi
{
    int32_t main4(int32_t argc, char *argv[]) {
        // Invoke the Java program with the passed arguments.
        /*string cmd = "java -cp OthelloFramework.jar TestGame";
        argv++;
        while (--argc) {
            cmd += " ";
            cmd += *(argv++);
        }
        return system(cmd.c_str());*/
        return 0;
    }
}
